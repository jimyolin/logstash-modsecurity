output {
  elasticsearch {
    hosts => localhost
    manage_template => true
    index => "modsecurity-%{+YYYY.MM}"

    # Change filepath before using
    template => "/etc/logstash/conf.d/templates/modsec_template.json"
    template_name => "modsec_template"
  }
}

